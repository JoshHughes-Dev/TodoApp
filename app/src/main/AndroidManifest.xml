<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jhughes.todoapp">

    <application
        android:name=".TodoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".ui.activity.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.ChooserActivity"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.Main"/>
        <activity
            android:name=".ui.activity.tasks.SimpleTasksActivity"
            android:theme="@style/AppTheme.Main"
            android:windowSoftInputMode="adjustResize"/>
        <activity
            android:name=".ui.activity.tasks.LiveDataTasksActivity"
            android:theme="@style/AppTheme.Main"
            android:windowSoftInputMode="adjustResize"/>
        <activity
            android:name=".ui.activity.tasks.PaperDbTasksActivity"
            android:theme="@style/AppTheme.Main"
            android:windowSoftInputMode="adjustResize"/>
        <activity
            android:name=".ui.activity.tasks.CoroutinesTasksActivity"
            android:theme="@style/AppTheme.Main"
            android:windowSoftInputMode="adjustResize"/>
        <activity
            android:name=".ui.activity.tasks.SuperTasksActivity"
            android:theme="@style/AppTheme.Main"
            android:windowSoftInputMode="adjustResize"/>
    </application>

</manifest>